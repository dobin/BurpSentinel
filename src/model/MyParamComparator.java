/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package model;

import java.util.Comparator;

/**
 *
 * @author unreal
 */
class MyParamComparator implements Comparator<SentinelHttpParam> {

    @Override
    public int compare(SentinelHttpParam o1, SentinelHttpParam o2) {
        //if (o1.getType() == o2.getType()) {
            return 0;
        //}
        
    }
    
}
